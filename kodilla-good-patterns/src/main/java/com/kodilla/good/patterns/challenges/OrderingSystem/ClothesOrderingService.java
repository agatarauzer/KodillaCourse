package com.kodilla.good.patterns.challenges.OrderingSystem;

import java.time.LocalDateTime;

public class ClothesOrderingService implements OrderingService {

    @Override
    public boolean order(User user, LocalDateTime date) {
        return true;
    }
}
